# dependencies
node_modules/
.pnp/
.pnp.js

# next
.next/
out/
build/

# misc
.DS_Store
*.pem
.env*.local

# data — generated by `python -m radar.export` and committed by the daily
# GitHub Actions workflow so Vercel can read it during `next build`.
# Locally, `npm run dev` will pick up whatever JSON the last export wrote.
